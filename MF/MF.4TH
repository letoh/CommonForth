// MetaForth Loader for Common Forth v1.660
NEEDS TURNKEY.4TH
: MF:LOAD&GO
	" MF.BIN" READ/ONLY OPEN IF
	   DUP $180000  512 K  ROT  HREAD IF
	      DROP HCLOSE DROP $180000 EXECUTE
	   ELSE
	      HERROR$ TYPE BEEP
	   ENDIF
	   HCLOSE DROP
	ELSE
	   HERROR$ TYPE BEEP
	ENDIF BYE ;

' MF:LOAD&GO TURNKEY MF

